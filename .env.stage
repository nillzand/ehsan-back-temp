# backend/.env.stage

# --- Django Settings for Staging ---
# DEBUG must be False in staging to mirror production
DJANGO_DEBUG=False

# IMPORTANT: Replace with your actual staging domain
ALLOWED_HOSTS=your-staging-domain.com,backend-staging-url.com

# SECRET_KEY: Generate a NEW, different secret key for staging
# You can use an online generator or Django's management command
SECRET_KEY='django-insecure-a-brand-new-secret-key-for-staging'

# --- Database URL for Staging ---
# This should be the connection string for your staging PostgreSQL database
# provided by your PaaS.
DATABASE_URL=postgres://user:password@staging-db-host:port/staging_db_name

# --- Frontend Build Arguments for Staging ---
# This is the PUBLIC URL of your staging backend that the frontend will call.
VITE_API_BASE_URL=https://your-staging-domain.com/api